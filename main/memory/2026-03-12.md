# 2026-03-12 Session Notes

## agent-files-spec → Expert Agent Format

- Converted AGENT-FILES-SPEC.md from static doc to expert agent format at `~/.openclaw/workspaces/shared/agents/agent-files-spec.md`
- Format: frontmatter (name/description/tools/model) + role definition + standards + report format
- Inspired by github.com/affaan-m/everything-claude-code/agents/ pattern
- model: opus, tools: Read/Grep/Glob
- Old file at shared/standards/AGENT-FILES-SPEC.md still exists — needs cleanup

## Key Decisions During Spec Refinement

- Guiding Principles removed (redundant with File Standards); agent-writable rule moved to Validated Patterns
- File Taxonomy table removed; Purpose/Owner merged into per-file specs
- Owner field removed (redundant with agent-writable principle)
- CHANGELOG.md removed from review scope
- sessions_send pattern removed (outdated, now using Task Board)
- "Own scope only" relaxed: shared rules MAY be duplicated but MUST stay consistent
- English-only rule added to Validated Patterns
- Size limits added: TOOLS.md ≤50 lines, USER.md ≤30 lines

## Full Review Completed — Key Findings

- 🔴 main/AGENTS.md: 137 lines (limit 100), needs compression
- 🔴 main/SOUL.md: "Role" section has operational details, should move to AGENTS.md
- 🔴 Chinese content across all agents — needs English translation
- 🟡 MEMORY.md non-standard sections (Gateway 操作规范, Pre-flight, etc.) — should fold into standard 4 sections
- 🟡 USER.md: all agents at 33 lines (limit 30)
- ✅ Cross-agent consistency: Task Board info, lifecycle, pre-flight all consistent

## File-by-File Review In Progress

### main/SOUL.md — Boss approved plan:
- Delete "Role" section; keep core identity sentence ("You are the orchestrator...") in Personality
- Delete "执行边界" section → move to AGENTS.md Operational Rules
- Move Behavior Rule #5 ("Track delegation progress") → AGENTS.md Operational Rules
- Move Boundary #4 ("Never claim completion without verification") → AGENTS.md Operational Rules
- Result: 4 Behavior Rules, 3 Boundaries — pending Boss final confirm to execute

## Spec Updates (evening session)

- Strengthened AGENTS.md spec: "verifiable and actionable — no rules that can't be checked for compliance"
- Added: "don't repeat system prompt built-in behaviors (memory management, silent replies, heartbeats)"
- Commits: 2d1d87a, 34ee313

## main/AGENTS.md Rewrite Progress

### Deleted (with Boss approval):
- Communication rules #1-3 (conclusion first / show output / cite sources) — #1 duplicates SOUL.md, #2-#3 too vague to verify
- Memory rules #1-2 (write MEMORY.md / update when corrected) — duplicates OpenClaw system prompt built-in memory management

### Key insight: OpenClaw system prompt already handles memory
- memory_search/memory_get tools built-in
- Pre-compaction memory flush is system prompt behavior
- No need to duplicate in AGENTS.md

### Current proposal: 5 principle-level rules (compressed from original 13)
1. Delegate first (execution boundary)
2. Verify before reporting
3. Recover before escalating
4. Task visibility (Topic + Task Board + status reporting)
5. Log non-git operations to CHANGELOG.md

### Pending: Boss deciding between 5-rule vs 10-rule version
- Boss asked "有没有最佳实践" — researched industry examples
- Industry pattern: role description + workflow, not numbered rule lists
- But numbered rules suit persistent orchestrator agents better (fast session load)
- Awaiting Boss decision on granularity

## Evening Session — SOUL.md + AGENTS.md Final Refinement

### SOUL.md agreed changes (Boss approved):
- Deleted Personality line "Concise. No fluff. Give conclusions directly." — merged into Rule #2
- Deleted "Driven and proactive. See the big picture, keep things moving." — behavioral, not personality
- Personality now 1 line: "You are the orchestrator..."
- Tone changed from 3-item list → single paragraph: "Concise and sharp. Decision-oriented..."
- Rule #2 became: "Be direct and concise — no hedging, give conclusions first"
- Rule #3 changed: "For ambiguity, research or ask — don't guess" (Boss: don't give guesses, find reliable answers)
- Rule #4 kept: "Push back when needed" → refined to "Challenge bad ideas — if the plan has problems, say so before executing"
- Added Rule #5: "Full transparency — all work goes through Topics and Task Board. No side channels, no hidden execution. Every action is visible, traceable, and auditable."
- Boundaries: added destructive operations confirm (rm -rf, force push, gateway restart); "don't make things up"
- Final: 20 lines

### AGENTS.md agreed changes (Boss approved):
- Title: "Task Delegation" → "Agents"
- Responsibilities: removed "Team coordinator" (duplicates SOUL.md "orchestrator"), now: "Think, plan, coordinate, and verify. Monitor sub-agents via Task Board and Topics..."
- Approval Flow: merged 2 items into 1 — "System changes and core file modifications: delegate → review → get user approval"
- Operational Rules settled at 4:
  1. Delegate first — at most 1 step to confirm, then delegate. Own scope: think, plan, coordinate, verify
  2. Verify before reporting
  3. Recover before escalating
  4. Task visibility (Topic + Task Board, cross-linked, real-time status)
- Rule #5 (CHANGELOG.md) dropped from rules — moved to Delegation details
- "Boss" → "user" throughout
- 76 lines total

### Pending decisions:
- Files not yet written to disk — awaiting final "写入" confirmation
- Remaining: MEMORY.md, USER.md, IDENTITY.md, dev/*, sysadmin/* review

## Late Night Session — Further Refinement (23:00-23:59)

### SOUL.md final version (20 lines, Boss approved):
- Personality: 1 line ("You are the orchestrator...")
- Tone: 1 paragraph ("Concise and sharp. Decision-oriented...")
- Behavior Rules: 4 rules
  1. Always delegate — never execute unless no sub-agent can handle it
  2. Be direct and concise — no hedging, give conclusions first
  3. For ambiguity, research or ask — don't guess
  4. Challenge bad ideas — if the plan has problems, say so before executing
- Boundaries: destructive ops confirm, no disclosure, don't make things up

### AGENTS.md final version (52 lines, Boss approved):
- Title: "Agents"
- Responsibilities: "Think, plan, coordinate, and verify. Monitor sub-agents via Task Board and Topics..."
- Approval Flow: 3 categories needing approval (system changes, core files with explicit list, destructive ops). Removed "Routine tasks: auto-execute" (unnecessary).
- Operational Rules: 5 rules
  1. Always delegate — never execute unless no sub-agent can
  2. Verify before reporting
  3. Recover before escalating
  4. Every task requires Topic + Task Board — check before starting, create if missing
  5. Real-time status updates (📋→🚀→📊→✅/❌), Task Board synced
- Delegation: Sub-agents (MaoGen, MaoYi) + Workflow (3 steps: Dispatch→Pick up→Execute)
- Removed "Don't Delegate" section (Rule #1 already defines scope)

### TOOLS.md changes (Boss approved):
- Task Board config (URL, app_token, table_id, fields) moved from AGENTS.md → TOOLS.md
- Group Chat config (ID, members, rules) moved from AGENTS.md → TOOLS.md
- These are tools, not delegation rules

### Workflow standardization:
- 3-step workflow: Dispatch → Pick up → Execute
- Each step specifies [Task Board] and [Topic] operations explicitly
- Cross-linking: Task Board stores Topic link, Topic stores Record ID
- This workflow applies to ALL agents (including Maoku) — copy to sub-agents
- All tasks go through Task Board (unified entry point, no exceptions)
- Bot-to-bot @mention doesn't work on Feishu — agents discover tasks via heartbeat polling Task Board
- Topic group (话题群) can only post in Topics, not general chat

### Key design decisions:
- SOUL.md "delegate first" (attitude) + AGENTS.md "always delegate" (operational rule) = intentional overlap, different layers
- SOUL.md Boundaries keeps "destructive ops confirm" even though AGENTS.md Approval Flow also lists it — one is attitude, one is process
- Removed "at most 1 step to confirm" from AGENTS.md Rule #1 — Responsibilities already defines scope
- Removed "own scope: think, plan, coordinate, verify" from Rule #1 — same reason
